# Public repo. Nothing here that carries faces, full-res, or client-private data
# may be committed until the releases decision is on file (see STATE.md).
_work/
img/web/
img/present/
img/thumb/
downloads/
forty_two.json
links.json
masters_delivery/
.DS_Store
__pycache__/
# The book layout renders camper faces onto every page. This repo is public and
# STATE.md records no releases, so the book ships as a PDF to the camp and never
# as a commit (hub CLAUDE.md, Images).
book/
